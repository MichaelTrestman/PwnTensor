apiVersion: v1
kind: Service
metadata:
  name: pwntensor-miner
  namespace: pwntensor
  labels:
    app: pwntensor
    component: miner
spec:
  selector:
    app: pwntensor
    component: miner
  ports:
    - name: axon
      port: 8091
      targetPort: 8091
      protocol: TCP
  type: ClusterIP
